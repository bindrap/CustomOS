# Greetd configuration for PBOS
# Using tuigreet with custom launch script for ASCII art

[terminal]
vt = 1

[default_session]
# Use a PBOS wrapper that shows animation + greeting, then runs tuigreet
command = "/usr/local/bin/pbos-greetd-start"
user = "greeter"
