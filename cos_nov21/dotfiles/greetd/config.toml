# Greetd configuration for CustomOS
# Login screen with GTK greeter running in cage compositor

[terminal]
vt = 1

[default_session]
# Run gtkgreet inside cage compositor for proper Wayland display
command = "cage -s -- gtkgreet"
user = "greeter"
