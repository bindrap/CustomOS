# Greetd configuration for CustomOS
# Using tuigreet for reliable text-based login

[terminal]
vt = 1

[default_session]
# Use tuigreet with Hyprland as default session
command = "tuigreet --time --remember --remember-session --cmd Hyprland"
user = "greeter"
